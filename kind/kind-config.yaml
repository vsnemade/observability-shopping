# KIND cluster for the observability lab.
# Single control-plane node is plenty. We reach the UIs with `kubectl port-forward`
# (see scripts/port-forward.sh), so no ingress/extraPortMappings are required.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: obs-lab
nodes:
  - role: control-plane
